import * as React from "react";
export const Button = (props) => {
    return (React.createElement(Button, { label: "Send", className: props.className, onClick: props.onClick }, props.label));
};
//# sourceMappingURL=button.js.map